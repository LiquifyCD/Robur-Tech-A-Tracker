{
  "name": "fondkoll",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Responsive fund search, tracking and comparison with explicit data-quality states.",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir .wrangler-dry-run",
    "check": "node scripts/check.mjs",
    "test": "node --test test/*.test.js",
    "verify": "npm run check && npm test && npm run build",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}
